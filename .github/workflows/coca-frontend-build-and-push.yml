# 1단계: Build 단계 (Node.js로 정적 파일 빌드)
FROM node:18 AS builder
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# 2단계: Serve 단계 (Nginx로 정적 파일 제공)
FROM nginx:alpine
COPY --from=builder /app/build /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
